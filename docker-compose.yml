services:
  qqauto:
    image: qqauto:latest
    container_name: qqauto
    restart: unless-stopped
    ports:
      - "8086:8086"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SERVER_PORT: 8086
      APP_CONTEXT_PATH: /qqauto
    volumes:
      - ./data:/app/data
